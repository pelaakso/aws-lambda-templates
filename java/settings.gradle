pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

rootProject.name = 'aws-lambda-templates'

include 'blank-java'
include 'blank-java-with-powertools'
